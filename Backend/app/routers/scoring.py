"""
Scoring endpoints for CV-JD compatibility calculation.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api", tags=["Scoring"])

# TODO: Move /api/calculate-score endpoint here from main.py
# This is a placeholder to allow the app to start
# The scoring logic is complex and should be migrated carefully
